# Build artifacts
target/
**/*.rs.bk
*.pem

# Anchor
.anchor/
test-ledger/

# IDL
target/idl/
target/types/

# Keypairs (NEVER commit these, except deployed program keypairs)
# Note: Negation patterns only work within the same directory scope.
# Since target/ is already ignored on line 2, we cannot re-include files within it.
# Deploy keypairs should be regenerated via anchor build/deploy.
*-keypair.json

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

